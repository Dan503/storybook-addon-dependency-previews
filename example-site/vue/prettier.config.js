//  @ts-check

/** @type {import('prettier').Config} */
const config = {
	semi: false,
	singleQuote: true,
	useTabs: true,
	trailingComma: 'all',
	plugins: ['prettier-plugin-tailwindcss'],
}

export default config
